import classNames from 'classnames';

export const newsClass = classNames(['flex flex-col gap-3']);

export const bucketClass = classNames(['overflow-hidden', 'bg-white', 'divide-y-2 divide-solid', 'rounded-lg']);

export const bucketTitleClass = classNames([
  'sticky top-0',
  'px-3 py-2',
  'text-blue-900 bg-white',
  'rounded-lg shadow-md',
]);
